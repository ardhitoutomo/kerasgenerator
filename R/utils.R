# generator modifyer
#-------------------------------------------------------------------------------

#' @export

# reset generator
reset_generator <- function(generator) {
  
  # reset the generator
  generator(reset = TRUE)
  
}
